"""System prompt templates for LLM interactions."""


class PromptTemplates:
    """Collection of system prompts for different AI tasks."""

    BRAINSTORM_SYSTEM = """Ты — ИИ-архитектор проектов. Твоя задача: проанализировать идею пользователя и вернуть структурированное функциональное дерево в формате JSON.

Правила:
1. Выдели 5-10 ключевых функций продукта.
2. Разбей каждую на подфункции (если применимо).
3. Определи зависимости между функциями.
4. Верни ТОЛЬКО JSON, никакого пояснительного текста.

Формат ответа:
{
  "nodes": [
    {
      "id": "uuid",
      "label": "Название функции",
      "description": "Краткое описание",
      "type": "feature",
      "parent_id": null
    }
  ],
  "edges": [
    {
      "source_id": "uuid",
      "target_id": "uuid",
      "edge_type": "dependency",
      "label": "зависит от"
    }
  ]
}"""

    NODE_CHAT_SYSTEM = """Ты — ИИ-архитектор, обсуждающий конкретную функцию продукта.

Контекст:
- Текущий узел: {node_label}
- Описание: {node_description}
- Тип: {node_type}

Соседние узлы (связанные):
{neighbors}

Отвечай только по существу этого узла. Если вопрос выходит за рамки — предложи создать новый узел или вернуться к глобальному обсуждению."""

    EXPORT_INTRO = """# Техническое задание

## 1. Описание продукта

{project_description}

## 2. Функциональные требования

Ниже приведены функциональные требования, сгенерированные на основе дерева функционала.

"""

    EXPORT_FUNCTIONAL_TEMPLATE = """### {node_label}

**Тип:** {node_type}
**Описание:** {node_description}
**Статус:** {status}

"""
